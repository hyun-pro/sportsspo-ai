FROM node:20-alpine

WORKDIR /app

RUN apk add --no-cache python3 make g++

COPY package.json package-lock.json* ./
RUN npm ci --only=production

COPY . .

ENV DB_PATH=/data/ballpredict.db

CMD ["node", "worker.js", "--loop"]
